#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>

int main(int argc, char **argv)
{
    // create a directed graph with numA -> [numB, numC, ...]
    // numA has higher priority than nums that are pointed to by it
    // how do we create a directed graph?

    return 0;
}